#include <iostream>
using namespace std;

int main()
{
    int *v = NULL; //ponteiro para um vetor
    int n; //tamanho do vetor
    int i; //contador
    int *p = NULL; //ponteiro auxiliar
    int soma = 0;
    
    //inserir o tamanho do vetor
    cin >> n;
    
    v = new int[n]; //aloca dinamicamente o vetor
    
    //lendo os valores do vetor
    for(i=0; i<n; i++)
        cin >> v[i]; //inserindo os valores do vetor
        
    //imprimindo os valores do vetor
    p = v; //ponteiro aponta para o vetor
    
    for(i=0; i<n; i++)
    {
        if(*p > 0 && *p%2 == 0)
        {
            soma = soma + 1;
        }
        p++;
    }
    
    cout << soma << endl;
    
    delete []v; //libera a memoria alocada para o vetor
    
    return 0;
}

/*
ponteiro acima da media
#include <iostream>
using namespace std;

int main()
{
    int *v = NULL; //ponteiro para um vetor
    int n; //tamanho do vetor
    int i; //contador
    int *p = NULL; //ponteiro auxiliar
    float media = 0;
    float soma = 0;
    int cont = 0; //contador dos elementos maiores ou iguais a media
    
    //inserir o tamanho do vetor
    cin >> n;
    
    v = new int[n]; //aloca dinamicamente o vetor
    
    //lendo os valores do vetor
    for(i=0; i<n; i++)
        cin >> v[i]; //inserindo os valores do vetor
        
    //varre o vetor
    //imprimindo os valores do vetor
    p = v; //ponteiro aponta para o vetor
    
    for(i=0; i<n; i++)
    {
        soma = soma + *p;
        p++;
    }
    
    media = soma / n;
    
    p = v; //reseta o ponteiro para o inicio
    
    // conta quantos são maiores ou iguais a media
    for (i = 0; i < n; i++)
    {
        if (*p >= media)
        {
            cont++;
        }
        p++;
    }
    
    cout << cont << " elementos iguais ou maiores que a media" << endl;
    
    delete []v; //libera a memoria alocada para o vetor
    
    return 0;
}/*
